#!/bin/sh

PATH=/bin:/usr/bin:/sbin:/usr/sbin

if test "x$FIND_OPTIONS" = "x"; then
  FIND_OPTIONS='-mtime 0'
fi

source libsrpmix.sh
find $SRPMIX_SOURCES_DIR/*/*/ -maxdepth 1 $FIND_OPTIONS | grep -v "/\$" | xargs -n1 dirname | sort | uniq | xargs -n1 --verbose /usr/sbin/hardlink


#!/bin/sh
#| -*- scheme -*- |#
:; exec gosh -- $0 "$@"
;; (libdwarves ...)
;; (dive-dwarf query)
(define (id->es compile-unit id)
  )
(define (es->id es)
  )
(define (register-id compile-unit id es)
  )


;; (?subroutine NAME)
(define (main args)
  
  )
; (define-class <compile-unit> ()
  
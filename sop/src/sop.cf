SOP_SOURCES_DIR=/srv/sources
SOP_ARCHIVES_DIR=${SOURCES_DIR}/archives
SOP_MIRROR_DIR=${ARCHIVES_DIR}/mirror
SOP_BACKUP_DIR=/backup



SOP_ENABLE_MIRROR=yes
SOP_MIRROR_TARGET_HOST=(
	"ftp.redhat.com"
)

SOP_MIRROR_TARGET_PATH=(
	"/pub/redhat"
)

SOP_MIRROR_TARGET_PATTERN=(
	'*.src.rpm'
)


SOP_ENABLE_BACKUP=yes
SOP_BACKUP_SRC=(
	"${MIRROR_DIR}/ftp.redhat.com"
	"${MIRROR_DIR}/extra"
)

SOP_BACKUP_DST=(
	"${BACKUP_DIR}/mirror/ftp.redhat.com"
	"${BACKUP_DIR}/mirror/extra"
)
